import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "ApexCare Solutions | Professional Cleaning & Property Care Ghana",

  description:
    "ApexCare Solutions provides reliable cleaning and property care services for homes, offices and commercial spaces across Ghana.",

  keywords: [
    "cleaning services Ghana",
    "office cleaning Accra",
    "property maintenance Ghana",
    "residential cleaning services",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
